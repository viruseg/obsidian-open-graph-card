{
  "description": "",
  "devDependencies": {
    "esbuild": "^0.27.3",
    "obsidian": "^1.12.3",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "esbuild main.ts --bundle --platform=node --external:obsidian --format=cjs --outfile=main.js"
  }
}
